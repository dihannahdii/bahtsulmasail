{
  "name": "bahtsul-masail-engine",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "vercel-build": "cd frontend && rm -rf node_modules/ && npm install && npx --yes vite build"
  }
}