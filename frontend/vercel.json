{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://bahtsulmasail-api.vercel.app/api/:path*"
    }
  ],
  "buildCommand": "rm -rf node_modules/ && sudo npm install && sudo npm run build",
  "outputDirectory": "dist"
}